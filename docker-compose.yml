
services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: soa-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - soa-network
    restart: unless-stopped

  redis:
    image: redis:alpine
    container_name: soa-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - soa-network
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: soa-backend
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=YOUR_SUPER_SECRET_JWT_KEY_FOR_SOFT_ONBOARDING_AGENT_256_BITS
      - APP_OAUTH2_REDIRECT_URI=/login
    env_file:
      - ./backend/.env
    networks:
      - soa-network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: soa-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - soa-network

networks:
  soa-network:
    driver: bridge

volumes:
  rabbitmq_data:
  redis_data:
